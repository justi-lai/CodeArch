# Exclude build metadata and configuration
.vscode/**
.vscode-test/**
.gitignore
vsc-extension-quickstart.md
tsconfig.json
eslint.config.mjs
package-lock.json
download-parsers.sh
.vscode-test.mjs

# Exclude source code
src/**

# Exclude development-only binaries/logs
**/*.map
**/*.ts
**/.DS_Store
**/*.vsix

# IMPORTANT: Ensure out/ and parsers/ are INCLUDED. 
# We use ! to allow them, but they must stay below the ignore list.
!out/**
!parsers/**

# Re-ignore maps that might have been picked up by the !out/** rule
out/**/*.map
